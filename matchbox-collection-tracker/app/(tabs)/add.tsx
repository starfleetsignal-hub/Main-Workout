// Intercepted by the tab bar's `add` tabPress listener, which pushes the
// `/add` modal route instead of navigating here — this screen never renders.
export default function AddTabPlaceholder() {
  return null;
}
