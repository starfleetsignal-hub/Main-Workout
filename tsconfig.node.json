{
  "//": "Compiles the parts of the app that run outside Metro (engine, broker adapters, license verifier) to ES modules, so the headless runner and the test suite can run them under plain Node. The app itself is bundled by Metro and does not use this output.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "outDir": "dist-esm",
    "rootDir": "src",
    "strict": true,
    "skipLibCheck": true,
    "declaration": false,
    "sourceMap": false,
    "types": ["node"]
  },
  "include": ["src/engine/**/*.ts", "src/broker/**/*.ts", "src/license/format.ts", "src/license/activation.ts"]
}
